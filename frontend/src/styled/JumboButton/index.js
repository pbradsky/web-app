import BootstrapButton from 'react-bootstrap/Button';
import styled from 'styled-components';

const JumboButton = styled(BootstrapButton)`
    &:hover {
      color: #27A745;
    }
`;

export default JumboButton;