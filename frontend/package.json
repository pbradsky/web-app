{
  "name": "web-app",
  "version": "0.2.0",
  "private": true,
  "dependencies": {
    "@smartcar/auth": "^2.5.0",
    "axios": "^0.19.0",
    "firebase": "^6.4.0",
    "query-string": "^6.8.2",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-router": "^5.0.1",
    "react-router-dom": "^5.0.1",
    "recompose": "^0.30.0"
  },
  "devDependencies": {
    "react-scripts": "3.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build-and-deploy": "npm run build && gcloud app deploy react-app.yaml --project jurne-test-web-app && echo 'Deployed ~ :)'"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
