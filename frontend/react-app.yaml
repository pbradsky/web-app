runtime: nodejs10
service: default

handlers:
  - url: /
    static_files: build/index.html
    upload: build/index.html
    secure: always
    script: auto
  - url: /
    static_dir: build
    secure: always
    script: auto
